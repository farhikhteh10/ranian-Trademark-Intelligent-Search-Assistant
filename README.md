# ⚖️ دستیار هوشمند جستجوی علائم تجاری (نسخه ۶ - هوش تجاری)
# Iranian Trademark Intelligent Search Assistant (BI Edition)

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![Selenium](https://img.shields.io/badge/Selenium-4.x-green)
![PyQt6](https://img.shields.io/badge/GUI-PyQt6-orange)
![Status](https://img.shields.io/badge/Status-Stable-brightgreen)

این نرم‌افزار یک ابزار پیشرفته **هوش تجاری (BI)** جهت اتوماسیون، آنالیز و استعلام وضعیت ثبت برند در سامانه مرکز مالکیت معنوی ایران است. برخلاف ربات‌های ساده، این سیستم مجهز به یک هسته پردازشی ۴ لایه است که رفتار کارشناسان اداره ثبت را شبیه‌سازی کرده و تعارضات پنهان برند را کشف می‌کند.

---

## ✨ ویژگی‌های کلیدی

* **رابط کاربری مدرن (GUI):** طراحی شده با PyQt6، دارای تم و فونت استاندارد (Vazirmatn)، پشتیبانی کامل از RTL.
* **آنالیز ۴ لایه (Forensic Analysis):** بررسی عمیق نام برند بر اساس تشابه آوایی، بصری، ترجمه و ریشه‌یابی.
* **دور زدن هوشمند محدودیت‌ها:** مدیریت سشن‌ها، تأخیر تصادفی (Random Delay) برای جلوگیری از مسدود شدن IP.
* **مدیریت کپچا (Semi-Auto):** نمایش تصویر کپچا داخل نرم‌افزار جهت ورود آسان توسط کاربر (بدون نیاز به مشاهده مرورگر).
* **استخراج داده‌های عمیق:** باز کردن خودکار نتایج، استخراج نام مالک، شماره ثبت و کالاها از تب‌های جدید یا مودال‌ها.
* **داشبورد مدیریتی:** تفکیک آنی نتایج به دو لیست "تأیید شده (آزاد)" و "رد شده (دارای تعارض)".
* **خروجی اکسل:** گزارش‌دهی دقیق و پاکسازی شده (Sanitized) جهت استفاده مدیریتی.

---

## 🧠 منطق آنالیز ۴ لایه (The 4-Layer Analysis Engine)

این نرم‌افزار صرفاً یک کلمه را جستجو نمی‌کند؛ بلکه آن را "کالبدشکافی" می‌کند:

### ۱. لایه آوایی (Homophones)
سامانه روی صدای کلمات حساس است. نرم‌افزار تمام حروف هم‌صدا را جایگزین می‌کند.
* مثال: ورودی «صبا» -> جستجوی خودکار: «سبا»، «ثبا».

### ۲. لایه بصری (Visual Similarity)
حروفی که در نوشتار فارسی شبیه هم هستند و ممکن است باعث خطای دید شوند.
* مثال: ورودی «پگاه» -> جستجوی خودکار: «بگاه» (تبدیل پ/ب، ک/گ، ج/چ، ی/ئ).

### ۳. لایه ترجمه و فینگلیش (Translation & Transliteration)
بررسی تعارض با معادل لاتین یا فینگلیش کلمه.
* مثال: ورودی «سیب» -> جستجوی خودکار: «Sib».
* *نکته:* اگر در فایل ورودی ترجمه را داخل پرانتز بنویسید (مثلاً `سیب (Apple)` )، ترجمه نیز جستجو می‌شود.

### ۴. لایه ریشه‌یابی و حذف زوائد (Descriptive Terms Removal)
حذف کلمات توصیفی که از نظر حقوقی "وجه تمایز" نیستند.
* مثال: ورودی «شرکت توسعه صنعت تک نان جنوب» -> نرم‌افزار کلمات (شرکت، توسعه، صنعت، جنوب) را حذف کرده و ریشه **«تک نان»** را آنالیز و جستجو می‌کند.

---

## 🚀 راهنمای نصب و اجرا

### پیش‌نیازها
* سیستم عامل ویندوز (تست شده روی Windows 10/11)
* پایتون نسخه ۳.۸ یا بالاتر
* مرورگر Google Chrome

۲. کتابخانه‌های مورد نیاز را نصب کنید:
```bash
pip install -r requirements.txt
```
اجرا

فایل اصلی برنامه را اجرا کنید:
```bash
python TrademarkApp_V6_BI.py
```

📖 نحوه استفاده
    یک فایل متنی (.txt) ایجاد کنید و نام‌های مورد نظر را در آن بنویسید (هر نام در یک خط).
    برنامه را اجرا کنید.
    روی دکمه "انتخاب فایل" کلیک کرده و فایل متنی را انتخاب کنید.
    دکمه "شروع آنالیز و جستجو" را بزنید.
    مرورگر کروم باز می‌شود. به محض لود شدن، تصویر کپچا در نرم‌افزار نمایش داده می‌شود.
    کد کپچا را در نرم‌افزار وارد کرده و اینتر بزنید.
    نتایج به صورت زنده در جدول و لیست‌های مدیریتی نمایش داده می‌شوند.
    در پایان، دکمه "خروجی اکسل" را برای دریافت گزارش نهایی بزنید.

🛠 تکنولوژی‌های استفاده شده

    Language: Python
    Browser Automation: Selenium WebDriver
    GUI Framework: PyQt6
    Data Manipulation: Pandas, OpenPyXL
    Logic: Custom Algorithms (itertools for permutation)

⚠️ سلب مسئولیت (Disclaimer)

این نرم‌افزار صرفاً جهت اهداف آموزشی و تسهیل فرآیند تحقیقات بازار توسعه یافته است.
مسئولیت استفاده صحیح از این ابزار بر عهده کاربر است.
لطفاً در استفاده از آن، قوانین و مقررات وب‌سایت هدف (ipm.ssaa.ir) را رعایت فرمایید.
ارسال درخواست‌های بیش از حد (DDoS) ممکن است منجر به مسدود شدن IP شما شود. (نرم‌افزار دارای مکانیزم تأخیر داخلی است، اما احتیاط شرط عقل است)
### نصب
۱. مخزن را کلون کنید:
```bash
git clone [https://github.com/farhikhteh10/Trademark-Search-Assistant.git]((https://github.com/farhikhteh10/ranian-Trademark-Intelligent-Search-Assistant/blob/main/TrademarkApp_V6_BI.py))
cd Trademark-Search-Assistant
